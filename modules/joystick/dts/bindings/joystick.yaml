description: Analog joystick with x/y potentiometers connected to ADC channels
compatible: joystick

include: base.yaml

properties:
  io-channels:
    type: phandle-array
    required: true
  io-channel-names:
    type: string-array
    required: true
